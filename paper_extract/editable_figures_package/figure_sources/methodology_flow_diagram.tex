\documentclass[tikz,border=8pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usetikzlibrary{arrows.meta,positioning,shapes.geometric,fit}

\begin{document}
\definecolor{ink}{RGB}{35,35,35}
\definecolor{softa}{RGB}{248,248,248}
\definecolor{softb}{RGB}{242,242,242}
\definecolor{softc}{RGB}{235,235,235}
\definecolor{softd}{RGB}{245,245,245}
\definecolor{groupc}{RGB}{150,150,150}
\begin{tikzpicture}[
    font=\sffamily,
    >=Latex,
    node distance=4.5mm and 4.5mm,
    line/.style={draw=ink, -Latex, line width=0.8pt, rounded corners=2pt},
    block/.style={draw=ink, rounded corners=5pt, align=center, minimum width=2.55cm, minimum height=0.82cm, fill=softb, text=ink, line width=0.85pt},
    detail/.style={draw=ink, rounded corners=5pt, align=left, text width=2.95cm, inner sep=4.5pt, fill=softa, text=ink, line width=0.75pt},
    branch/.style={draw=ink, rounded corners=5pt, align=left, text width=2.55cm, inner sep=4.5pt, fill=softc, text=ink, line width=0.75pt},
    note/.style={draw=ink, rounded corners=5pt, align=left, text width=3.05cm, inner sep=4.5pt, fill=softd, text=ink, line width=0.75pt},
    group/.style={draw=groupc, dashed, rounded corners=7pt, inner sep=6pt, line width=0.8pt}
]

\node[block] (start) {\textbf{Methodology}};

\node[detail, below=of start] (model) {\textbf{1. Plant model}\\
Nominal DC motor\\
state-space form\\
$u \in [-24,24]$ V};

\node[detail, below=of model] (stress) {\textbf{2. Conditions}\\
6 cases:\\
nominal, $R_a{+}50\%$, $J{+}50\%$\\
friction, saturation, combined};

\node[block, below=of stress] (controllers) {\textbf{3. Controllers}};

\node[branch, below left=7mm and 4mm of controllers] (classical) {\textbf{Classical}\\
PID, LQR, LQI\\
MPC, $H_\infty$\\
No retuning};

\node[branch, below=7mm of controllers] (directrl) {\textbf{Direct RL}\\
TD3, DDPG, SAC\\
Full-voltage action\\
Shaped reward};

\node[branch, below right=7mm and 4mm of controllers] (residual) {\textbf{Residual SAC}\\
Backbone: LQR/MPC/$H_\infty$\\
$u=\mathrm{clip}(u_b+\Delta u)$\\
$\Delta u \in [-8,8]$ V};

\node[note, below=10mm of directrl] (training) {\textbf{4. Training}\\
MPC warm-start\\
curriculum learning\\
10 seeds, 100k steps\\
best checkpoint};

\node[detail, below=of training] (protocol) {\textbf{5. Benchmark}\\
4 references:\\
step, disturbed step\\
ramp, sinusoid\\
$4 \times 6 = 24$ cases};

\node[detail, below=of protocol] (metrics) {\textbf{6. Metrics}\\
IAE, SSE, ISE\\
energy, saturation\\
rise, settling\\
overshoot};

\node[block, below=of metrics] (output) {\textbf{7. Comparison}};

\draw[line] (start) -- (model);
\draw[line] (model) -- (stress);
\draw[line] (stress) -- (controllers);
\draw[line] (controllers) -- (classical);
\draw[line] (controllers) -- (directrl);
\draw[line] (controllers) -- (residual);
\draw[line] (classical.south) |- (training.west);
\draw[line] (directrl) -- (training);
\draw[line] (residual.south) |- (training.east);
\draw[line] (training) -- (protocol);
\draw[line] (protocol) -- (metrics);
\draw[line] (metrics) -- (output);

\node[group, fit=(classical)(directrl)(residual), label={[text=ink]above:\textbf{Controller Families}}] {};

\end{tikzpicture}
\end{document}
